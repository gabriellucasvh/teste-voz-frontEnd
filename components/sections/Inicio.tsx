import React from 'react';

const Inicio = () => {
  return (
    <section className='bg-no-repeat bg-top bg-contain lg:bg-cover w-full h-[30vh] lg:h-[110vh]' style={{backgroundImage: 'url(/fundo.svg)'}}>
      {/* Aqui está definido o fundo principal do menu */}
    </section>
  );
};

export default Inicio;
